const CustomLoader = () => {
  return (
    <div
      className={`text-white bg-slate-800/50 absolute inset-0 flex__center z-50`}
    >
      Loading..........
    </div>
  );
};

export default CustomLoader;
